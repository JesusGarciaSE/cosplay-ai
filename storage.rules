rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true; // Public read for MVP; tighten later
      allow write: if request.auth != null; // Auth required for writes
    }
  }
}

