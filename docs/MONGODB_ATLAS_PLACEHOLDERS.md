# MongoDB Atlas placeholders (to complete later)

- Create a free cluster in MongoDB Atlas
- Create a database user and get a connection string
- Whitelist 0.0.0.0/0 temporarily or set specific IPs
- Paste the URI into `functions/.env` as `MONGO_URI`
- For production, use Firebase Secret Manager instead of plain `.env`

